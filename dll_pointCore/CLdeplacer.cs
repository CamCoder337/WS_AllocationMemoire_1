namespace dll_pointCore;

public class CLdeplacer
{
    // Public Function deplacer(point() As Double) As Double(){}
    // Dim newpoint(2) As Double
    // newpoint(0) = point(0) + 1
    // newpoint(1) = point(1) + 1
    // Return newpoint

}